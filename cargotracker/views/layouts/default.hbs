<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    {{#each styles}}
        <link rel = 'stylesheet' href={{this}}/>
    {{/each}}
</head>
  <body>
  {{> header}}
    {{{body}}}
  </body>
</html>
